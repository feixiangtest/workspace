import * as types from './type'

export const state = {
  userChips: [] // 用户筹码
}

export const mutations = {
    /**
     *
     * @param {*} state
     * @param {*} data
     */
  [types.CHIPS_SAVE_MUTATION] (state, res) {
  }
}
